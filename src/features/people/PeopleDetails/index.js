import { Section } from "../../../common/Section";
import { SectionHeader } from "../../../common/SectionHeader";

const PeopleDetails = () => (
  <Section>
    <SectionHeader>Movie details</SectionHeader>
  </Section>
);

export default PeopleDetails;